<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Libraries Listed</title>
  </head>
  <body>
    <h1>All These Are Listed</h1>
    <% for(let l of libraries){ %>
    <li><a href="/library/<%= l.id %>"><%= l.name %></a> - <%= l.price %></li>
    <% } %>
    <footer>
      <a href="/library/new">Add New Library</a>
    </footer>
  </body>
</html>
